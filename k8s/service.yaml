apiVersion: v1
kind: Service
metadata:
  name: sentiment-service
spec:
  selector:
    app: sentiment-api
  ports:
    - protocol: TCP
      port: 80          # Service port
      targetPort: 8000  # Container port where app listens
  type: ClusterIP
